version: 2.1

jobs:
  build:
    docker:
      - image: cimg/openjdk:21.0.6

    steps:
      - checkout
      - run:
          name: "Build"
          command: "./gradlew build"
          no_output_timeout: 60s
workflows:
  build-workflow:
    jobs:
      - build