version: 2.1

jobs:
  build:
    docker:
      - image: cimg/openjdk:21.0.6

    steps:
      - checkout
      - run:
          name: "Build"
          command: "./gradlew build"

workflows:
  build-workflow:
    jobs:
      - build